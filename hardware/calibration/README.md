# Calibration

Measured per-unit values: camera intrinsics, sensor extrinsics, IMU bias, wheel odometry scale.

These are specific to one physical rover, not to the design. If you build a second one, it gets its own file.
